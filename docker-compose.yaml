version: '3.8'

services:
  aro-proposal:
    build: .
    container_name: aro-proposal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
